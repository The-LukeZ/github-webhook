{
  "name": "github-webhook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "wrangler build --minify",
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "bulk-env": "wrangler secret bulk .dev.vars"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "typescript": "^5.5.2",
    "wrangler": "^4.61.0"
  },
  "dependencies": {
    "discord-api-types": "^0.38.37"
  }
}
